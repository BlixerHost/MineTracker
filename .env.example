# ─── MineTracker — Variables de entorno ──────────────────────────────────────
# Copia este archivo a apps/api/.env y rellena los valores.
# NUNCA subas .env a git.

# ─── Entorno ──────────────────────────────────────────────────────────────────
NODE_ENV=production

# ─── Base de datos (MariaDB) ──────────────────────────────────────────────────
# setup.sh crea el usuario/base automáticamente. Solo cambia el host si usas
# un servidor externo de MariaDB (por defecto localhost:3306).
DATABASE_URL="mysql://minetracker:CAMBIA_ESTO_DB_PASSWORD@localhost:3306/minetracker"

# Contraseña del usuario "minetracker" en MariaDB (genera con: openssl rand -hex 24)
DB_PASSWORD=CAMBIA_ESTO_openssl_rand_hex_24
# Contraseña root de MariaDB (solo para docker-compose)
DB_ROOT_PASSWORD=CAMBIA_ESTO_openssl_rand_hex_24

# ─── Redis (opcional — sin él no hay caché ni rate-limit de submissions) ──────
REDIS_URL=""
# REDIS_URL="redis://localhost:6379"

# ─── Autenticación ────────────────────────────────────────────────────────────
# PRODUCCIÓN: mínimo 64 chars. Genera con: openssl rand -hex 64
JWT_SECRET=CAMBIA_ESTO_openssl_rand_hex_64
JWT_EXPIRES_IN=8h

# ─── Seguridad ────────────────────────────────────────────────────────────────
# PRODUCCIÓN: mínimo 32 chars. Genera con: openssl rand -hex 32
IP_HASH_SALT=CAMBIA_ESTO_openssl_rand_hex_32

# ─── API ──────────────────────────────────────────────────────────────────────
API_PORT=3001
CORS_ORIGINS=https://tudominio.com
APP_URL=https://tudominio.com
API_URL=http://localhost:3001

# ─── Admin (solo para el seed inicial) ───────────────────────────────────────
ADMIN_EMAIL=admin@tudominio.com
ADMIN_PASSWORD=ContraseñaMuySegura123!

# ─── Submissions ──────────────────────────────────────────────────────────────
ENABLE_SERVER_SUBMISSIONS=true
SUBMISSION_RATE_LIMIT_PER_HOUR=5
REQUIRE_ADMIN_APPROVAL=true

# ─── Worker ───────────────────────────────────────────────────────────────────
PING_INTERVAL_SECONDS=10
PING_CONCURRENCY=50
PING_TIMEOUT_MS=5000
